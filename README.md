# 外贸报价单发票生成器

一个离线使用的外贸报价单、形式发票、商业发票、装箱单和利润核算工具。项目不依赖服务器，数据保存在当前浏览器本地。

本仓库为 **网页开源版**，使用 MIT License。Windows 客户端、客户端打包脚本及企业定制版本不属于本开源仓库的发布范围。

当前版本：v1.1.0

## 功能

- 生成报价单、形式发票、商业发票和装箱单
- 报价记录 / 草稿管理：保存、打开、复制和删除历史报价
- 数据备份与恢复：导出 JSON 备份文件，换电脑或重装浏览器后可导入恢复
- 支持产品图片、Logo、贸易条款、银行信息和备注
- 支持打印或另存为 PDF
- 支持导出 Excel
- 包含利润核算、包装数据管理和物流费用管理

## 使用方法

### 下载

1. 打开本项目的 GitHub 页面。
2. 点击页面右上方绿色的 `Code` 按钮。
3. 点击 `Download ZIP`。
4. 下载完成后，右键压缩包，选择“全部解压”或“解压到当前文件夹”。

### Windows 启动

1. 打开解压后的文件夹。
2. 再打开里面的 `报价单生成器` 文件夹。
3. 双击 `QuotationGenerator.cmd`。
4. 如果 Windows 弹出安全提示，选择“更多信息”，再选择“仍要运行”。

如果无法启动，请确认电脑已安装 Chrome 或 Microsoft Edge。

请不要把 `报价单生成器/index.html` 这段文字复制到浏览器地址栏。它是项目里的文件位置，不是网页链接。

## 报价记录 / 草稿管理

1. 在报价单页面填写内容后，点击顶部的 `保存`，当前报价会保存到本机的报价记录。
2. 点击 `另存`，可以把当前内容复制成一条新的报价记录。
3. 点击顶部的 `记录`，可以搜索历史报价，支持按客户、单号、产品名称查找。
4. 在报价记录页面可以打开编辑、复制为新报价或删除记录。

## 数据备份与恢复

1. 点击顶部的 `备份`。
2. 点击 `导出 JSON 备份`，把当前电脑里的报价记录、草稿、包装和物流数据保存成备份文件。
3. 需要恢复时，点击 `选择 JSON 备份文件`，选择之前导出的备份文件。
4. 导入恢复会覆盖当前本机数据，操作前建议先导出一次当前数据。

## 隐私说明

本工具默认不上传数据，也不连接外部接口。填写的公司、客户、报价、银行和物流信息会保存在当前浏览器的本地存储中。分享给他人前，请不要把自己浏览器里的导出文件、截图或已填写资料一起打包。

开源版本已清理默认公司信息、Logo 和银行信息。使用者需要自行填写自己的资料。

## 开发说明

这是一个纯前端项目，不需要安装依赖即可运行。主要文件：

- `报价单生成器/index.html`：单据生成器
- `报价单生成器/app.js`：单据生成逻辑
- `报价单生成器/calculator.html`：利润核算器
- `报价单生成器/calculator.js`：利润核算逻辑
- `报价单生成器/packaging.html`、`报价单生成器/packaging.js`：包装数据管理
- `报价单生成器/logistics.html`、`报价单生成器/logistics.js`：物流费用管理

## 开源协议

本项目使用 MIT License。

MIT License 允许使用、修改、分发和商业使用本仓库中的开源代码，具体以仓库中的 `LICENSE` 为准。

## Windows 客户端与定制支持

如需 Windows 客户端、企业专属模板或功能定制，请联系：

- 作者：Barry
- 邮箱：932518135@qq.com
- 微信：klm2202
- 联系时请备注“外贸报价单发票生成器”。
